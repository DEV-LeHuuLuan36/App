@model App.Models.YTa

@{
    ViewData["Title"] = "Xác nhận Xóa Y tá";
}

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-danger">@ViewData["Title"]</h6>
    </div>
    <div class="card-body">
        <h3>Bạn có chắc muốn XÓA y tá này?</h3>
        <p class="text-danger">Cảnh báo: Không thể xóa nếu y tá này đang phụ trách theo dõi bệnh nhân.</p>
        <hr />
        <dl class="row">
            <dt class="col-sm-3">Mã Y tá</dt>
            <dd class="col-sm-9">@Model.MaYTa</dd>
            <dt class="col-sm-3">Tên Y tá</dt>
            <dd class="col-sm-9">@Model.TenYTa</dd>
            <dt class="col-sm-3">Phòng</dt>
            <dd class="col-sm-9">@Model.MaPhongNavigation.TenPhong</dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="MaYTa" />
            <input type="submit" value="Xác nhận Xóa" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
        </form>
    </div>
</div>